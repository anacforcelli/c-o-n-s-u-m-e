; consume - Basic template config file

; Loads regular config data
@consume.ini@

; Overrides some values for profile
[Display]
Title           = consume (Profile)

[Render]
ShowFPS         = true
