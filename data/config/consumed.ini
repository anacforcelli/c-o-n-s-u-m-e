; consume - Basic template config file

; Loads regular config data
@consume.ini@

; Overrides some values for debug
[Display]
Title           = consume (Debug)

[Render]
ShowFPS         = true

[Resource]
WatchList       = Config # Texture # Sound
